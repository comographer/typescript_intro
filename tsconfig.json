{
  // inform which folder uses typescript
  "include": ["src"],
  // inform where the compiled javascript file should be saved on
  "compilerOptions": {
    // javascript files will be saved on the folder "build"
    "outDir": "build",
    // javascript will be compiled to "ES6"
    "target": "ES6"
  }
}
